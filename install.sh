### Install entry script
### Install Postgres 1st, then Kamailio, then FreeSWITCH, then Bun, and the API server.
# I am still working on the API server, so it is not yet ready for production use. The API server is a work in progress and will be updated as I continue to develop it.
# we probably need to install other security features like fail2ban, ufw, and other security features to harden the server. Go ahead and add a list of security features to install and configure. I will work on the security features later, but for now, we will focus on getting the basic installation working. We want this to be a production ready VOIP PBX system that's secure and performant.
